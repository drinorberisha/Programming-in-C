 
#include <stdio.h>
int main() {
    //the problem with the code was that the variable result, first should be assigned with a value and then make the calculation
    double result = ( 3 + 1 );
     result = result / 5;
 
     printf("The value of 4/5 is %lf\n", result);
     return 0;
     }